// created a constant variable Kelvin and assign vallue to it
const Kelvin=0;
// created another const variable celsius and asiign value to it
const Celsius=Kelvin-273;

// create an var type variable fahrenhite it can mutable
var fahrenheit=Celsius*(9/5)+32;
// converting decimal to whole no
fahrenheit=Math.floor(fahrenheit);

console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);

var Newton=Celsius * (33/100);
Newton=Math.floor(Newton);
console.log(Newton);